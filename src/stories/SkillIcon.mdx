import { Meta, Story, Canvas, Controls } from '@storybook/blocks';
import { SkillIcon } from '../components/SkillIcon';
import * as SkillIconStories from './SkillIcon.stories.tsx';

<Meta of={SkillIconStories} />

# SkillIcon Component

A flexible React component for displaying programming and technology skill icons.

## Usage

<Canvas>
  <Story of={SkillIconStories.Default} />
</Canvas>

<Controls />

## Size Variations

Different sizes can be applied using the `size` prop:

<Canvas>
  <Story of={SkillIconStories.Sizes} />
</Canvas>

## Color Customization

Icons can be customized with different colors:

<Canvas>
  <Story of={SkillIconStories.Colors} />
</Canvas>

## Available Icons

Here's a gallery of all available icons:

<Canvas>
  <Story of={SkillIconStories.AllIcons} />
</Canvas>

## Props

| Prop | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| name | string | - | The name of the icon to display |
| size | number | 24 | Size of the icon in pixels |
| color | string | - | Color of the icon (hex, rgb, etc.) |
| showLabel | boolean | true | Whether to show the icon label |
| labelClassName | string | - | Additional classes for the label |
| className | string | - | Additional classes for the icon wrapper |
| textSize | `'xs'` \| `'sm'` \| `'base'` \| `'lg'` \| `'xl'` \| `'2xl'` \| `'3xl'` \| `'4xl'` \| `'5xl'` | `'xs'` | Size of the label text |