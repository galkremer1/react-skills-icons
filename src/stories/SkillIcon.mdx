import { Meta, Story, Canvas } from '@storybook/blocks';
import * as SkillIconStories from './SkillIcon.stories';

<Meta of={SkillIconStories} />

import { SkillIcon } from '../components/SkillIcon';
import { iconMappings } from '../constants/iconMappings';



# SkillIcon Component

A flexible icon component for displaying programming and technology skills.

## Features

*   100+ technology and skill icons
*   Customizable size and colors
*   Optional labels
*   TypeScript support

## Basic Usage

<Canvas>
<Story name="Basic">
<div className="flex gap-4">
<SkillIcon name="react" />
<SkillIcon name="typescript" />
<SkillIcon name="javascript" />
</div>
</Story>
</Canvas>

## Size Variations

<Canvas>
<Story name="Sizes">
<div className="flex items-center gap-4">
<SkillIcon name="react" size={16} />
<SkillIcon name="react" size={24} />
<SkillIcon name="react" size={32} />
<SkillIcon name="react" size={48} />
</div>
</Story>
</Canvas>

## Color Customization

<Canvas>
<Story name="Colors">
<div className="flex gap-4">
<SkillIcon name="react" color="#61DAFB" />
<SkillIcon name="react" color="#0284c7" />
<SkillIcon name="react" color="#404040" />
</div>
</Story>
</Canvas>

## All Available Icons

<Canvas>
<Story name="Icon Gallery">
<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(100px, 1fr))', gap: '16px' }}>
{iconMappings.map((icon) => (
<div key={icon.name} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', padding: '8px', border: '1px solid #ddd', borderRadius: '4px' }}>
<SkillIcon name={icon.name} size={32} />
<span style={{ marginTop: '4px', fontSize: '0.75rem' }}>{icon.name}</span>
</div>
))}
</div>
</Story>
</Canvas>

The `SkillIcon` component accepts the following props:

| Prop | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| name | string | - | The name of the icon to display |
| size | number | 24 | Size of the icon in pixels |
| color | string | - | Color of the icon (hex, rgb, etc.) |
| showLabel | boolean | true | Whether to show the icon label |
| labelClassName | string | - | Additional classes for the label |
| className | string | - | Additional classes for the icon wrapper |
| textSize | `'xs'` \| `'sm'` \| `'base'` \| `'lg'` \| `'xl'` \| `'2xl'` \| `'3xl'` \| `'4xl'` \| `'5xl'` | `'xs'` | Size of the label text |